<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || t("app_name") %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="<%= t("app_name") %>">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag "application", "devise", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="<%= 'devise-page' if devise_controller? %>">
    <% if devise_controller? %>
      <div class="devise-wrap">
        <%= yield %>
      </div>
    <% else %>
      <header class="app-header">
        <div class="app-header-inner">
          <%= link_to t("app_logo"), root_path, class: "app-logo" %>
          <nav class="app-nav">
            <%= link_to t("layout.home"), root_path, class: (controller.controller_name == "home" ? "active" : "") %>
            <%= link_to t("layout.plants"), plants_path, class: (controller.controller_name == "plants" ? "active" : "") %>
            <%= link_to t("layout.animals"), animals_path, class: (controller.controller_name == "animals" ? "active" : "") %>
            <%= link_to t("layout.consumable_items"), consumable_items_path, class: (controller.controller_name == "consumable_items" ? "active" : "") %>
            <%= link_to t("layout.stock_lots"), stock_lots_path, class: (controller.controller_name == "stock_lots" ? "active" : "") %>
            <%= button_to t("layout.sign_out"), destroy_user_session_path, method: :delete, class: "btn btn-secondary" %>
          </nav>
        </div>
      </header>

      <main class="app-main">
        <% if flash.any? %>
          <div class="flash-messages">
            <% flash.each do |type, message| %>
              <div class="flash flash-<%= type %>"><%= message %></div>
            <% end %>
          </div>
        <% end %>

        <%= yield %>
      </main>
    <% end %>
  </body>
</html>
