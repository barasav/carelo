<% content_for :title, t("consumption_rules.edit_title") %>

<div class="page-header">
  <h1 class="page-title"><%= t("consumption_rules.edit_title") %></h1>
  <p class="page-subtitle"><%= @animal.name %></p>
</div>

<%= render "form", animal: @animal, consumption_rule: @consumption_rule %>
